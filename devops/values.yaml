repositoryName: helpdesk-phone
repositoryOwner: commexcom
imageTag: 5263a3d247b56f2ef37d637435c872d82223d356-stage
replicaCount: 2
service:
  enabled: true
  type: ClusterIP
  internalPort: 3000
  externalPort: 443
ingress:
  enabled: true
  className: "nginx"
  annotations: {nginx.ingress.kubernetes.io/ssl-redirect: "true", nginx.ingress.kubernetes.io/force-ssl-redirect: "true", nginx.ingress.kubernetes.io/rewrite-target: /}
  hosts:
    - host: stage-telefone.guaruja.sp.gov.br
      paths:
        - path: /
          pathType: Prefix
  tls:
    - hosts:
        - stage-telefone.guaruja.sp.gov.br
      secretName: guarujaspgovbr-tls
